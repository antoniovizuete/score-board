#ifndef __DATA_H__
#define __DATA_H__
// Structure example to send data
// Must match the receiver structure
typedef struct scores_message {
  unsigned int scoreA;
  unsigned int scoreB;
} scores_message;
#endif